// SCRATCH FILE — safe to delete.
//
// Created on 2026-09-20 while probing vitest's handling of factory mocks, and
// left behind because the sandbox's safety guard refused the `rm` that should
// have removed it. Nothing imports this; it exists only so the file is valid
// TypeScript while it waits to be deleted.
export {};
