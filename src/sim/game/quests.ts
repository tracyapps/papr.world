// Superseded placeholder.
//
// The quest runtime lives at `src/game/quests.ts`. This file exists only
// because an authoring step briefly wrote it to the wrong path
// (`src/sim/catalogs/../game/` resolves to `src/sim/game/`, not `src/game/`).
// It exports nothing and nothing imports it. `src/sim/game/` can be deleted
// once a shell is handy.
export {};
