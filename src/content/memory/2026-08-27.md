# 2026-08-27

## papr.world critter content expansion
- Expanded `src/content/conversations.json` for the pencil-and-paper game:
  - Everyday pools roughly doubled (greetings/self/trait/place facts) + new "Any news?" everyday choice (`replyMode: random`).
  - 7 personality one-shots (one per trait) — engine `personalities` filter matches EITHER personality slot, so dramatic/gentle/mischievous/sleepy critters (which can only be secondary traits) still get their scenes.
  - 4 friendship-gated scenes: stranger intro (friendshipLevels), friend/buddy/pet scenes (minFriendship). Pet "quiet hour" is maxPlays 3 with 3 rotating openings.
  - 7 multi-step side-quest arcs (flag-chained like Bandit's Moon Button): Chisel Wind Ledger (chains off new `chisel:met` flag added to BOTH mill-welcome choices), squirrel Acorn Census, bunny Meadow Regatta, cat Midnight Snack Club, bird Duet of Small Winds, butterfly Bloom Map, raccoon Scrapflats Edge-Matching.
- Engine facts learned: primary personality is only ever shy/bold/curious (from `generateCritterParams`); `friendship` choice field is POINTS (levels: curious 10/friend 30/buddy 60/pet 90, cap 100); critter ids are `px,pz#species`; named critters: Bandit `0,0#raccoon`, Chisel `-2,0#woodchuck`; templates {{name}}/{{region}}/{{biome}} + catalog placeholders {{material:...}} etc.
- Verified: JSON valid, all requiresFlags chains resolve, each quest arc has exactly one terminal `:complete` step, full vitest suite 435/435 passes.
